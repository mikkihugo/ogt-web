[mysqld]
# Local-only MariaDB: no network, unix socket only (override at runtime if needed)
binlog_format=ROW
default-storage-engine=innodb
innodb_autoinc_lock_mode=2
skip-networking=ON
socket=/run/mysqld/mysqld.sock
bind-address=127.0.0.1
log_error=/dev/stderr
wsrep_on=OFF
wsrep_provider=none
