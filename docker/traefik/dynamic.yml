http:
  routers:
    ogt-web:
      rule: "PathPrefix(`/`)"
      service: ogt-web
      entryPoints:
        - web
  services:
    ogt-web:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:9000"
